import React from "react";

import * as S from "./styled";

export const Deal = () => {
	return (
		<S.DealContainer>
			how it works → your idea💡 → call cooper now or schedule for later →
			meet with a⚡c → approve proposal → we get the job done → you = 😁
		</S.DealContainer>
	);
};
